<ul class="list-group">
  <% @users.each do |user| %>

    <li class="list-group-item">
      <%= link_to user.name, user %> - <%= link_to 'Follow', follow_path(id: user.id), method: :post %>
      <span class="badge"><%= user.followers_count %></span>
    </li>

  <% end %>
</ul>
