<div id="interesting-users">
  <ul class="list-group">
    <% @users.each do |user| %>

      <li class="list-group-item">
        <%= user.name %> - <%= link_to 'Follow', follow_path(id: user.id), method: :post %>
      </li>

    <% end %>
  </ul>
</div>
